---
phase: 02-deploy-logic
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - d2p/src/deploy/verify.rs
  - d2p/src/deploy/mod.rs
autonomous: true
requirements: [DEP-04, DEP-05, OUT-01, OUT-04]

must_haves:
  truths:
    - "Runner::deploy() calls check_prerequisites() before anything else"
    - "check_prerequisites() fails fast with actionable message when forge or cast are not on PATH"
    - "Actionable error message says 'install Foundry: https://getfoundry.sh'"
    - "After primary or fallback succeeds, verify::verify() is called with the tx_hash and rpc_url"
    - "verify::verify() compares receipt status to '0x1', not to '1 (success)'"
    - "Runner::deploy() falls back to fallback::run() when primary::run() returns Err, with warning on stderr"
    - "DeployOutput::fmt writes only address and tx_hash to stdout — no diagnostic noise"
  artifacts:
    - path: "d2p/src/deploy/verify.rs"
      provides: "cast receipt --json status check"
      exports: ["pub fn verify(tx_hash: &str, rpc_url: &str) -> anyhow::Result<()>"]
    - path: "d2p/src/deploy/mod.rs"
      provides: "Runner struct + deploy() orchestration + check_prerequisites()"
      exports: ["pub struct Runner", "impl Runner { pub fn new(), pub fn deploy() }"]
  key_links:
    - from: "d2p/src/deploy/mod.rs Runner::deploy()"
      to: "deploy::primary::run()"
      via: "match primary::run(&self.params)"
      pattern: "primary::run"
    - from: "d2p/src/deploy/mod.rs Runner::deploy()"
      to: "deploy::verify::verify()"
      via: "verify::verify(&out.tx_hash, &self.params.rpc_url)?"
      pattern: "verify::verify"
    - from: "d2p/src/deploy/verify.rs verify()"
      to: "cast receipt --json"
      via: "Command::new(cast).args([receipt, --rpc-url, rpc_url, tx_hash, --json])"
      pattern: "\"receipt\""
---

<objective>
Implement `verify.rs` (receipt status check) and extend `deploy/mod.rs` with the `Runner` struct, `check_prerequisites()`, and the `deploy()` orchestration method that wires primary, fallback, and verify together.

Purpose: This completes Phase 2. After this plan, `Runner::deploy(params)` is a fully working, testable function — the CLI in Phase 3 only needs to construct a `DeployParams` and call it.

Output: `d2p/src/deploy/verify.rs` with a `pub fn verify()` and unit tests for JSON status parsing; `d2p/src/deploy/mod.rs` extended with `Runner`, `check_prerequisites()`, and `Runner::deploy()` with inline unit tests.
</objective>

<execution_context>
@/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-RESEARCH.md
</execution_context>

<context>
@/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/PROJECT.md
@/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/ROADMAP.md

<interfaces>
<!-- From Plan 01 output (now must exist before this plan runs) -->

From d2p/src/deploy/primary.rs (created in Plan 01):
```rust
pub fn run(params: &DeployParams) -> anyhow::Result<DeployOutput>
```

From d2p/src/deploy/fallback.rs (created in Plan 01):
```rust
pub fn run(params: &DeployParams) -> anyhow::Result<DeployOutput>
```

From d2p/src/deploy/mod.rs (Phase 1, extended by Plan 01 with mod declarations):
```rust
pub struct DeployParams { pub rpc_url, pub private_key, pub callback, pub value, pub contract_path, pub project_dir }
pub struct DeployOutput { pub address, pub tx_hash }
// pub mod primary;    (added by Plan 01)
// pub mod fallback;   (added by Plan 01)
```

From d2p/src/errors.rs:
```rust
pub enum D2pError {
    ProcessNotFound(String),
    NonZeroExit { stderr: String },
    ParseFailure(String),
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: verify.rs — cast receipt --json status check with JSON parsing tests</name>
  <files>d2p/src/deploy/verify.rs</files>
  <read_first>
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-RESEARCH.md (Pattern 3: Receipt Verification, Pitfall 3: positional field returning human string)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/errors.rs
  </read_first>

  <behavior>
    RED phase — write these tests first:
    - `test_verify_receipt_success`: parse `{"status":"0x1","contractAddress":"0xABC"}` — verify() with a mocked stdout returns Ok(())
      Implementation note: verify() takes stdout bytes not a Command — refactor the function to accept `&[u8]` internally via a `parse_receipt_status(stdout: &[u8]) -> anyhow::Result<()>` helper, and test that helper directly. The public `verify()` calls the Command then delegates to this helper.
    - `test_verify_receipt_reverted`: parse `{"status":"0x0","contractAddress":null}` — returns Err with message containing "reverted"
    - `test_verify_receipt_missing_status`: parse `{"blockNumber":"1"}` (no status field) — returns Err with message containing "missing status"
    - `test_verify_rejects_human_string`: parse `{"status":"1 (success)"}` — returns Err (the human-readable form must NOT be accepted; only "0x1" is valid per Pitfall 3)

    GREEN phase — implement verify.rs as shown in RESEARCH.md "Receipt Verifier" code example.
  </behavior>

  <action>
    Create `d2p/src/deploy/verify.rs`:

    1. Private `fn parse_receipt_status(stdout: &[u8]) -> anyhow::Result<()>`:
       - Parses stdout bytes as JSON: `serde_json::from_slice::<serde_json::Value>(stdout)`
       - Reads `v["status"].as_str()`
       - If `Some("0x1")` → Ok(())
       - If `Some(s)` → `anyhow::bail!("transaction reverted on-chain (status={s})")`
       - If `None` → `anyhow::bail!("cast receipt JSON missing status field")`

    2. `pub fn verify(tx_hash: &str, rpc_url: &str) -> anyhow::Result<()>`:
       - Spawns: `Command::new("cast").args(["receipt", "--rpc-url", rpc_url, tx_hash, "--json"]).output()`
         NOTE: `--rpc-url rpc_url` and `tx_hash` and `--json` are all in a flat slice. Arg order verified in RESEARCH.md Pattern 3.
       - On IO error → `context("cast receipt: cast not found")`
       - On non-zero exit → `anyhow::bail!("cast receipt failed: {}", stderr)`
       - Delegates to `parse_receipt_status(&out.stdout)`

    3. `#[cfg(test)] mod tests` with 4 unit tests from behavior block — all call `parse_receipt_status()` directly with byte slices, no subprocess spawn.

    Add `pub mod verify;` to `d2p/src/deploy/mod.rs`.
  </action>

  <verify>
    <automated>cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p && cargo test verify 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test verify` passes with 4 tests: test_verify_receipt_success, test_verify_receipt_reverted, test_verify_receipt_missing_status, test_verify_rejects_human_string
    - `grep -n "0x1" d2p/src/deploy/verify.rs` shows exact string comparison to `"0x1"` (not "1 (success)")
    - `grep -n "\"receipt\"" d2p/src/deploy/verify.rs` shows "receipt" as first arg to cast
    - `cargo check` exits 0
  </acceptance_criteria>

  <done>verify.rs compiles, 4 unit tests pass, only "0x1" is accepted as success, human-string "1 (success)" is rejected</done>
</task>

<task type="tdd">
  <name>Task 2: Runner + check_prerequisites() in deploy/mod.rs — orchestration with fallback and prerequisite tests</name>
  <files>d2p/src/deploy/mod.rs</files>
  <read_first>
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-RESEARCH.md (Pattern 4: PATH Check, Pattern 5: Runner::deploy() orchestration)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/mod.rs (current content — extend, do not overwrite)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/primary.rs (verify it exists and exports run())
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/fallback.rs (verify it exists and exports run())
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/verify.rs (verify it exists and exports verify())
  </read_first>

  <behavior>
    RED phase — write these tests first (in mod.rs `#[cfg(test)]`):
    - `test_check_prerequisites_missing_tool`: check_prerequisites() with a tool name that does not exist on PATH returns Err where the error message contains "getfoundry.sh" (DEP-04)
    - `test_check_prerequisites_bad_name`: verify the error message contains the tool name (so user knows which binary is missing)
    - `test_runner_fallback_warning_format`: the warn message printed to stderr in Runner::deploy() when forge fails contains "forge create failed" and "cast send --create" — tested by checking the eprintln! format string in source code (grep-based acceptance criterion)
    - `test_deploy_output_display_no_newline_suffix`: `DeployOutput { address: "0xA", tx_hash: "0xB" }.to_string()` equals exactly `"0xA\n0xB"` — no trailing newline (OUT-01, OUT-04). This test already passes from Phase 1 but must remain green.

    GREEN phase — add Runner to mod.rs as shown in RESEARCH.md Pattern 5.
  </behavior>

  <action>
    Extend (NOT overwrite) `d2p/src/deploy/mod.rs` by appending after the existing Display impl and tests:

    1. `pub fn check_prerequisites() -> anyhow::Result<()>` (free function, not method):
       Loop over `["forge", "cast"]`, for each:
       - `Command::new(tool).arg("--version").output()`
       - `Ok(_)` → continue
       - `Err(e) if e.kind() == std::io::ErrorKind::NotFound` → `anyhow::bail!("{tool} not found on PATH — install Foundry: https://getfoundry.sh")`
       - `Err(e)` → `anyhow::bail!("{tool}: {e}")`

    2. `pub struct Runner { params: DeployParams }` (no pub on field — params is private)

    3. `impl Runner`:
       - `pub fn new(params: DeployParams) -> Self { Runner { params } }`
       - `pub fn deploy(&self) -> anyhow::Result<DeployOutput>`:
         ```
         check_prerequisites()?;
         let out = match primary::run(&self.params) {
             Ok(o) => o,
             Err(e) => {
                 eprintln!("[warn] forge create failed ({e}), retrying with cast send --create");
                 fallback::run(&self.params)?
             }
         };
         verify::verify(&out.tx_hash, &self.params.rpc_url)?;
         Ok(out)
         ```

    4. Extend `#[cfg(test)] mod tests` (keep existing Phase 1 tests, add the 3 new behavior tests).

    IMPORTANT: Read the current mod.rs first to preserve existing content. Only append new items — do not replace DeployParams, DeployOutput, Display impl, or existing tests.

    After writing, also update `d2p/src/main.rs` to expose the deploy module publicly if needed: `main.rs` currently has `mod deploy;` and `mod errors;` — these already cover compilation. No main.rs change needed.
  </action>

  <verify>
    <automated>cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p && cargo test 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - Full `cargo test` suite passes (all Wave 1 tests + 4 verify tests + new runner tests)
    - `grep -n "getfoundry.sh" d2p/src/deploy/mod.rs` shows the install URL in check_prerequisites error
    - `grep -n "forge create failed" d2p/src/deploy/mod.rs` shows the fallback warning format string
    - `grep -n "verify::verify" d2p/src/deploy/mod.rs` shows verify is called inside Runner::deploy()
    - `grep -n "check_prerequisites" d2p/src/deploy/mod.rs` shows it is the first call in Runner::deploy()
    - `cargo build 2>&1` exits 0 (binary compiles)
    - Existing test `test_deploy_output_display` still passes (no regression)
  </acceptance_criteria>

  <done>Runner::deploy() is implemented, check_prerequisites() returns actionable error with getfoundry.sh URL, verify::verify() is called after every deployment, full cargo test suite is green</done>
</task>

</tasks>

<verification>
Phase 2 gate check — run after both tasks complete:

```bash
cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p && cargo test -- --include-ignored 2>&1
```

Expected: all tests green. Count should be at minimum: 3 (Phase 1) + 5 (primary) + 5 (fallback) + 4 (verify) + 3 (runner) = 20 tests.

Structural checks:
```bash
grep -rn "getfoundry.sh" /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/
grep -rn "env_remove.*ETH_RPC_URL" /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/
grep -rn "\"0x1\"" /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/verify.rs
```
</verification>

<success_criteria>
1. `Runner::deploy(params)` is callable from a Rust test and returns `anyhow::Result<DeployOutput>` (Phase 2 ROADMAP success criterion 1)
2. check_prerequisites() fails fast with "install Foundry: https://getfoundry.sh" when forge/cast absent (success criterion 5)
3. verify::verify() is called and compares status to "0x1" — not to "1 (success)" (success criterion 3)
4. DeployOutput::display() produces only address + tx_hash, no trailing noise (success criteria 1, 4)
5. Full `cargo test` is green — no regressions from Phase 1
</success_criteria>

<output>
After completion, create `/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-deploy-logic-02-SUMMARY.md`
</output>
