---
phase: 02-deploy-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - d2p/src/deploy/primary.rs
  - d2p/src/deploy/fallback.rs
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03, DEP-06]

must_haves:
  truths:
    - "forge create is called with --constructor-args as the last argument"
    - "forge create includes --legacy, --broadcast, --value, --rpc-url, --private-key, --json"
    - "cast send --create has all flags before --create (flags-before-subcommand order)"
    - "cast send args have no --legacy flag"
    - "Both paths remove ETH_RPC_URL from subprocess environment"
    - "fallback returns actionable error when artifact file is missing"
    - "Both paths parse structured JSON output via serde_json, not text-splitting"
  artifacts:
    - path: "d2p/src/deploy/primary.rs"
      provides: "forge create invocation + JSON parsing"
      exports: ["pub fn run(params: &DeployParams) -> anyhow::Result<DeployOutput>"]
    - path: "d2p/src/deploy/fallback.rs"
      provides: "cast send --create invocation + bytecode reading + JSON parsing"
      exports: ["pub fn run(params: &DeployParams) -> anyhow::Result<DeployOutput>"]
  key_links:
    - from: "d2p/src/deploy/primary.rs build_args()"
      to: "forge create subprocess"
      via: "Command::new(forge).args(&args).env_remove(ETH_RPC_URL)"
      pattern: "env_remove.*ETH_RPC_URL"
    - from: "d2p/src/deploy/fallback.rs build_args()"
      to: "cast send --create subprocess"
      via: "Command::new(cast).args(&args)"
      pattern: "\"--create\""
---

<objective>
Implement the two subprocess strategy modules — `primary.rs` for `forge create` and `fallback.rs` for `cast send --create` — with complete arg builders, JSON output parsers, and unit tests for every correctness constraint.

Purpose: These two modules are the entire deployment mechanism. Every requirement in Phase 2 that touches arg ordering, flag presence/absence, or output parsing is tested here before the Runner wires them together in Plan 02.

Output: `d2p/src/deploy/primary.rs` and `d2p/src/deploy/fallback.rs`, each with a `pub fn run()`, private arg builder, JSON deserialization structs, and inline `#[cfg(test)]` unit tests covering all arg-order constraints.
</objective>

<execution_context>
@/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-RESEARCH.md
</execution_context>

<context>
@/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/PROJECT.md
@/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/ROADMAP.md

<interfaces>
<!-- Phase 1 types used by both modules. No codebase exploration needed. -->

From d2p/src/deploy/mod.rs:
```rust
pub struct DeployParams {
    pub rpc_url: String,
    pub private_key: String,
    pub callback: String,
    pub value: String,
    pub contract_path: String,   // e.g. "src/.../UniswapV3Reactive.sol:UniswapV3Reactive"
    pub project_dir: PathBuf,
}

pub struct DeployOutput {
    pub address: String,
    pub tx_hash: String,
}
```

From d2p/src/errors.rs:
```rust
pub enum D2pError {
    ProcessNotFound(String),
    NonZeroExit { stderr: String },
    ParseFailure(String),
}
```

From d2p/Cargo.toml:
- serde_json = "1"   (already present — no Cargo.toml changes needed)
- anyhow = "1"
- thiserror = "2"
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: primary.rs — forge create module with arg-order tests</name>
  <files>d2p/src/deploy/primary.rs</files>
  <read_first>
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-RESEARCH.md (complete primary.rs implementation + pitfalls)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/mod.rs (DeployParams, DeployOutput)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/errors.rs (D2pError variants)
  </read_first>

  <behavior>
    RED phase — write these tests first, confirm they fail to compile (file does not exist):
    - `test_forge_args_order`: build_args() returns vec where index 0 is "create", index 1 is contract_path, and the last three elements are "--constructor-args", callback value (proving DEP-06)
    - `test_forge_args_contains_legacy`: "--legacy" appears in the arg vec (DEP-01)
    - `test_forge_args_contains_broadcast`: "--broadcast" appears in the arg vec (DEP-01)
    - `test_forge_args_no_env_inheritance`: Command builder calls env_remove("ETH_RPC_URL") — tested by inspecting that build_args does NOT contain "ETH_RPC_URL" as a string argument (the env_remove call is in run(), structural test)
    - `test_parse_forge_json`: ForgeCreateJson deserializes from `{"deployedTo":"0xABC","transactionHash":"0xDEF","deployer":"0x123"}` and maps to correct fields

    GREEN phase — implement the file exactly as shown in RESEARCH.md "Complete primary.rs" code example.
  </behavior>

  <action>
    Create `d2p/src/deploy/primary.rs` with the following exact structure (from RESEARCH.md, verified Foundry v1.5.1):

    1. Private `ForgeCreateJson` struct with `serde::Deserialize`, fields `deployed_to` (rename "deployedTo") and `transaction_hash` (rename "transactionHash")

    2. `pub fn run(params: &DeployParams) -> anyhow::Result<DeployOutput>`:
       - Calls `build_args(params)`
       - Spawns `Command::new("forge").args(&args).current_dir(&params.project_dir).env_remove("ETH_RPC_URL").output()`
       - Maps `io::ErrorKind::NotFound` to `D2pError::ProcessNotFound("forge".to_string())`
       - On non-zero exit: returns `Err(D2pError::NonZeroExit { stderr }.into())`
       - Parses `out.stdout` with `serde_json::from_str::<ForgeCreateJson>`, maps error to `D2pError::ParseFailure`
       - Returns `DeployOutput { address: parsed.deployed_to, tx_hash: parsed.transaction_hash }`

    3. Private `fn build_args(params: &DeployParams) -> Vec<String>`:
       CRITICAL arg order (DEP-01, DEP-06, verified against Foundry issue #770):
       ```
       ["create", contract_path, "--rpc-url", rpc_url, "--private-key", private_key,
        "--broadcast", "--legacy", "--value", value, "--json",
        "--constructor-args", callback]
       ```
       "--constructor-args" and callback MUST be the last two elements.

    4. `#[cfg(test)] mod tests` with the 5 unit tests from the behavior block.
       Tests call `build_args()` with a test `DeployParams` — no subprocess spawn needed.

    Add `pub mod primary;` to `d2p/src/deploy/mod.rs` (add line after existing content).
    Add `use anyhow::Context;` import at the top of primary.rs.
  </action>

  <verify>
    <automated>cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p && cargo test primary 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test primary` passes with 5 tests: test_forge_args_order, test_forge_args_contains_legacy, test_forge_args_contains_broadcast, test_forge_args_no_env_inheritance (or equivalent structural), test_parse_forge_json
    - `grep -n "constructor-args" d2p/src/deploy/primary.rs` shows "--constructor-args" appearing as the second-to-last push in build_args
    - `grep -n "env_remove" d2p/src/deploy/primary.rs` shows `env_remove("ETH_RPC_URL")`
    - `grep -n "legacy" d2p/src/deploy/primary.rs` shows "--legacy" present in build_args vec
    - `cargo check` exits 0 (no compile errors)
  </acceptance_criteria>

  <done>primary.rs compiles, 5 unit tests pass, --constructor-args is last in build_args, --legacy is present, ETH_RPC_URL is env_removed</done>
</task>

<task type="tdd">
  <name>Task 2: fallback.rs — cast send --create module with bytecode reading and arg-order tests</name>
  <files>d2p/src/deploy/fallback.rs</files>
  <read_first>
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-RESEARCH.md (complete fallback.rs + Pitfall 2 on arg order, Pitfall 5 on missing artifact)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/mod.rs (DeployParams, DeployOutput)
    - /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/errors.rs (D2pError)
  </read_first>

  <behavior>
    RED phase — write these tests first:
    - `test_cast_args_no_legacy`: build_args() vec does NOT contain "--legacy" anywhere (DEP-03)
    - `test_cast_args_order`: All of "--rpc-url", "--private-key", "--value", "--json" appear BEFORE "--create" in the vec; bytecode string appears immediately after "--create"; "constructor(address)" appears after bytecode; callback appears last (DEP-02, Pitfall 2)
    - `test_parse_cast_json`: CastSendJson deserializes from `{"contractAddress":"0xABC","transactionHash":"0xDEF","status":"0x1"}` and maps fields correctly
    - `test_read_bytecode_missing_artifact`: read_bytecode returns Err when artifact path does not exist, and error message contains "forge build" (Pitfall 5 actionable error)
    - `test_read_bytecode_missing_field`: read_bytecode returns Err when JSON exists but `bytecode.object` is absent

    GREEN phase — implement fallback.rs exactly as in RESEARCH.md "Complete fallback.rs" code example.
  </behavior>

  <action>
    Create `d2p/src/deploy/fallback.rs` with the following exact structure (from RESEARCH.md, verified Foundry v1.5.1):

    1. Private `CastSendJson` struct with `serde::Deserialize`, fields `contract_address` (rename "contractAddress") and `transaction_hash` (rename "transactionHash"). The `status` field from the receipt is NOT used here — receipt verification is handled separately in verify.rs (Plan 02). Only extract address and tx_hash.

    2. `pub fn run(params: &DeployParams) -> anyhow::Result<DeployOutput>`:
       - Calls `read_bytecode(params)?`
       - Calls `build_args(params, &bytecode)`
       - Spawns `Command::new("cast").args(&args).current_dir(&params.project_dir).env_remove("ETH_RPC_URL").output()`
       - Maps `io::ErrorKind::NotFound` to `D2pError::ProcessNotFound("cast".to_string())`
       - On non-zero exit: returns `Err(D2pError::NonZeroExit { stderr }.into())`
       - Parses `out.stdout` via `serde_json::from_str::<CastSendJson>`, maps error to `D2pError::ParseFailure`
       - Returns `DeployOutput { address: parsed.contract_address, tx_hash: parsed.transaction_hash }`

    3. Private `fn read_bytecode(params: &DeployParams) -> anyhow::Result<String>`:
       - Extracts `contract_name` from `params.contract_path.split(':').last()`
       - Builds artifact path: `params.project_dir / "out" / "{name}.sol" / "{name}.json"`
       - Reads file with `std::fs::read_to_string(&artifact).with_context(|| format!("artifact not found at {} — run `forge build` from {} first", ...))`
       - Parses JSON, extracts `v["bytecode"]["object"].as_str()`
       - Returns the string (includes 0x prefix — pass as-is to cast)

    4. Private `fn build_args(params: &DeployParams, bytecode: &str) -> Vec<String>`:
       CRITICAL order (Pitfall 2 — flags BEFORE --create):
       ```
       ["send", "--rpc-url", rpc_url, "--private-key", private_key,
        "--value", value, "--json",
        "--create", bytecode, "constructor(address)", callback]
       ```
       NO "--legacy" anywhere (DEP-03).

    5. `#[cfg(test)] mod tests` with the 5 unit tests from the behavior block.
       test_read_bytecode_missing_artifact and test_read_bytecode_missing_field use `tempfile`-free approach: construct a PathBuf to a known non-existent path.

    Add `pub mod fallback;` to `d2p/src/deploy/mod.rs`.
    Add `use anyhow::Context;` import.
  </action>

  <verify>
    <automated>cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p && cargo test fallback 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test fallback` passes with 5 tests: test_cast_args_no_legacy, test_cast_args_order, test_parse_cast_json, test_read_bytecode_missing_artifact, test_read_bytecode_missing_field
    - `grep -n "legacy" d2p/src/deploy/fallback.rs` returns nothing (no --legacy in fallback)
    - `grep -n -- "--create" d2p/src/deploy/fallback.rs` shows "--create" appearing after "--json" in build_args
    - `grep -n "env_remove" d2p/src/deploy/fallback.rs` shows `env_remove("ETH_RPC_URL")`
    - `grep -n "forge build" d2p/src/deploy/fallback.rs` shows actionable error message in read_bytecode
    - `cd d2p && cargo test 2>&1 | grep -E "^test result"` shows all tests passing
  </acceptance_criteria>

  <done>fallback.rs compiles, 5 unit tests pass, no --legacy in args, all flags precede --create, missing artifact returns actionable error message</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p && cargo test 2>&1
```

Expected: 10 new tests pass (5 primary + 5 fallback) plus 3 existing Phase 1 tests = 13 total green.

Also confirm module declarations exist:
```bash
grep -n "pub mod primary\|pub mod fallback" /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/d2p/src/deploy/mod.rs
```
</verification>

<success_criteria>
1. `cargo test primary` and `cargo test fallback` both pass independently
2. `--constructor-args` is provably last in primary.rs build_args (test_forge_args_order asserts this)
3. `--legacy` is provably absent from fallback.rs build_args (test_cast_args_no_legacy asserts this)
4. Flags-before-subcommand order in fallback is provably correct (test_cast_args_order asserts this)
5. Missing artifact produces error containing "forge build" (test_read_bytecode_missing_artifact asserts this)
6. Full `cargo test` green (no regressions from Phase 1)
</success_criteria>

<output>
After completion, create `/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.planning/phases/02-deploy-logic/02-deploy-logic-01-SUMMARY.md`
</output>
